# Server SMTP configuration (rename to .env and fill values)
# Example values shown; replace with your real SMTP provider credentials.

# SMTP server host
SMTP_HOST=smtp.example.com

# SMTP server port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP auth user (email or username)
SMTP_USER=your_email@example.com

# SMTP auth password
SMTP_PASS=gvlv yopo kgab ggte

# Destination email where contact form messages will be delivered
TO_EMAIL=ramkumar9219447537@gmail.com

# Optional: server port
PORT=4000

# Frontend: override backend API URL (Vite env var)
# Example: VITE_BACKEND_URL=http://localhost:4000/api/send
VITE_BACKEND_URL=http://localhost:4000/api/send
